# Windows application validation

Run `scripts/validate-windows-applications.ps1` from an interactive signed-in Windows desktop. It writes a sanitized result under `.test-runtime/validation/` and never force-terminates an application. Real desktop validation has not been performed by automated tests.
