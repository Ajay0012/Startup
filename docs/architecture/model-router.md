# Model Router

Known local commands use deterministic execution. Uncertain short language work can select Gemini FAST; research selects PRIMARY; repository analysis selects CODING; image work selects VISION only after sanitization and any required user confirmation. LOCAL_ONLY and REJECT content never routes to cloud. Provider failure and open circuits fall back to deterministic behavior where it exists.
